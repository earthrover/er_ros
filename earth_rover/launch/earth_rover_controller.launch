<launch>
	<rosparam file="$(find earth_rover)/config/earth_rover_control_test.yaml" command="load"/>

	<!-- Load the controller -->
	<node name="controller_spawner" pkg="controller_manager" type="spawner" args="robot_controller joint_state_controller --shutdown-timeout 3"/>

</launch>
